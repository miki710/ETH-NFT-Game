{
  "name": "eth-nft-game",
  "version": "1.0.0",
  "description": "Creating NFT Game",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "start": "react-scripts start",
    "contract": "yarn workspace contract",
    "client": "yarn workspace client",
    "test": "yarn workspace contract test"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}
